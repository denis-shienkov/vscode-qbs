export enum QbsRequestType {
    Build = 'build-project',
    Cancel = 'cancel-job',
    Clean = 'clean-project',
    GetRunEnvironment = 'get-run-environment',
    Install = 'install-project',
    Resolve = 'resolve-project',
}
