/** Helper enumeration for wrapping the command echo modes for Qbs protocol. */
export enum QbsProtocolCommandEchoMode {
    CommandLine = 'command-line',
    CommandLineWithEnvironment = 'command-line-with-environment',
    Silent = 'silent',
    Summary = 'summary',
}
